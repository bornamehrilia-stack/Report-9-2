# Report-9-2



---

# 📋 گزارش پروژه: تشخیص گاز با سنسور MQ-9 (Gas Detection System using MQ-9 Sensor)

## ۱. مقدمه  
در این پروژه, یک **سنسور گاز MQ-9** به آردوینو متصل شده است و مقدار ولتاژ خروجی آن (که متناسب با غلظت گاز است) را اندازه‌گیری می‌کند. اگر مقدار خوانده‌شده از سنسور از یک **آستانه تعیین‌شده** (در اینجا `30`) بیشتر باشد, آردوینو پیام **«GAS DETECTED!»** را در Serial Monitor چاپ کرده و یک **LED روشن** می‌کند. در غیر این صورت, LED خاموش می‌ماند.

> 🔍 توجه: سنسور MQ-9 برای تشخیص گازهایی مانند **CO (MONOXIDE)**، **LPG** و **گازهای قابل اشتعال** طراحی شده است.

  

## ۳. قطعات مورد استفاده  
- برد آردوینو Uno  
- سنسور گاز **MQ-9**  
- یک LED (در تصویر سبز/زرد روشن است)  
- یک مقاومت 220 اهم  
- برد برد و سیم‌های جامپر  

> ✅ این پروژه نیاز به سنسور MQ-9 و LED دارد.
 



## ۵. کد برنامه (اسکچ) 

```cpp
int led = 2;          // پین LED
int mq9Pin = A0;      // پین ورودی آنالوگ سنسور
int gasThreshold = 30; // آستانه تشخیص (مقداری که بالاتر از آن، گاز تشخیص داده می‌شود)

void setup() {
  pinMode(led, OUTPUT);   // تنظیم پین LED به عنوان خروجی
  Serial.begin(9600);     // شروع ارتباط سریال
  Serial.println("MQ9 Gas Sensor Test"); // پیام راه‌اندازی
}

void loop() {
  int sensorValue = analogRead(mq9Pin);  // خواندن مقدار آنالوگ (0 تا 1023)

  Serial.print("Sensor Value: ");
  Serial.println(sensorValue);           // نمایش مقدار در Serial

  // بررسی آیا مقدار از آستانه بیشتر است
  if (sensorValue > gasThreshold) {
    Serial.println("GAS DETECTED!");      // پیام هشدار
    digitalWrite(led, HIGH);              // LED روشن
  } else {
    digitalWrite(led, LOW);               // LED خاموش (پیش‌فرض)
  }

  delay(1000);  // تأخیر 1 ثانیه برای ثبات
}
```


## ۶. نحوه‌ی کارکرد  
1. پس از آپلود کد, سنسور شروع به اندازه‌گیری می‌کند.  
2. اگر گازی در محیط وجود داشته باشد (مثلاً با نزدیک کردن یک منبع گاز یا حتی نفس)، مقدار سنسور افزایش می‌یابد.  
3. وقتی مقدار از `30` بیشتر شود → LED روشن می‌شود و پیام هشدار نمایش داده می‌شود.  
4. اگر گاز از بین برود → مقدار کاهش یافته و LED خاموش می‌شود.

## ۷. نتایج  
- سنسور به درستی گاز را تشخیص می‌دهد.  
- LED به درستی روشن/خاموش می‌شود.  
- پیام‌ها در Serial Monitor به صورت صحیح نمایش داده می‌شوند.
.

---

